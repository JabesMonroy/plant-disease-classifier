# Inferencia en el Hugging Face Space.
# Las versiones de torch/executorch deben coincidir con las usadas al exportar el .pte.
# gradio lo instala el Space según 'sdk_version' del README (no se fija aquí).
executorch==0.4.0
torch==2.5.0
torchvision==0.20.0
